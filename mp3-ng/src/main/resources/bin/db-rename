#!/bin/bash

DIR=$1
if [ -z "$DIR" ]
then
  echo "Usage: $0 <DIR>"
  exit 1
fi

if [ ! -d "$DIR" ]
then
  echo "Dir $DIR does not exist!"
  exit 2
fi

exiftool -v '-FileName<CreateDate' -d %Y%m%d_%H%M%S%%-c-%%f.%%e $DIR

